package background;

import app.controls.CourseItem;
import weblearning.FileEntry;

public class DownloadManager {
    public static void enqueue(CourseItem courseItem, FileEntry entry) {

    }

    public static void enqueue(CourseItem courseItem, FileEntry[] entries) {

    }
}
